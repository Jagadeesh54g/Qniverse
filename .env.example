# Optional: enables MongoDB persistence
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@CLUSTER.mongodb.net/qniverse?retryWrites=true&w=majority

# Required for signed login sessions
AUTH_SECRET=replace-with-a-long-random-secret

# Optional: enables the AI tutor
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
