export async function POST(req){try{const {question,context}=await req.json();if(!question)return Response.json({answer:'Ask me a quantum question.'},{status:400});if(!process.env.OPENAI_API_KEY){return Response.json({answer:localTutor(question,context)})}const r=await fetch('https://api.openai.com/v1/chat/completions',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${process.env.OPENAI_API_KEY}`},body:JSON.stringify({model:process.env.OPENAI_MODEL||'gpt-5-mini',temperature:.2,messages:[{role:'system',content:'You are Qniverse, a rigorous but beginner-friendly quantum computing tutor. Explain intuition first, then mathematics. Never pretend a simulation result is known if it is not in context. When circuit context is supplied, reason from it. Encourage experimentation rather than simply giving answers.'},{role:'user',content:`Question: ${question}\nContext: ${JSON.stringify(context||{})}`} ]})});const j=await r.json();if(!r.ok)throw new Error(j.error?.message||'OpenAI request failed');return Response.json({answer:j.choices?.[0]?.message?.content||'No answer returned.'})}catch(e){return Response.json({answer:'The AI tutor could not reach the model right now. Check your API key/model configuration. Built-in explanations remain available in the lab.',error:String(e.message||e)},{status:200})}}
function localTutor(q){const x=q.toLowerCase();if(x.includes('superposition'))return 'Superposition means a qubit can be represented as α|0⟩ + β|1⟩, with |α|² + |β|² = 1. The Hadamard gate turns |0⟩ into an equal-amplitude superposition. Try H in the Lab and run 1,000 shots.';if(x.includes('entangle'))return 'A simple Bell-state experiment is H(q0) followed by CNOT(q0,q1). H creates the superposition; CNOT correlates the second qubit with the first. Try it and look for 00 and 11 outcomes.';if(x.includes('h²')||x.includes('twice'))return 'Hadamard is its own inverse: H·H = I. Applying H twice returns each computational-basis state to itself. Verify it experimentally with two H gates on q0.';return 'Start with one question, build the smallest circuit that tests it, run several shots, and compare the result with your prediction. That experiment is often the fastest route to intuition.'}
