'use client';
import Link from 'next/link';import {lessons,challenges} from '@/lib/content';import {useProgress} from '@/lib/progress';
export default function Progress(){const {data}=useProgress();const lessonDone=Object.keys(data.lessons).length;const challDone=Object.keys(data.challenges).length;const pct=Math.round((lessonDone+challDone)/(lessons.length+challenges.length)*100);return <div className="container page"><div className="progress-hero"><div><div className="eyebrow">YOUR QNIVERSE</div><h1>Momentum becomes<br/><em>mastery.</em></h1><p>Qniverse remembers the concepts you complete and the challenges you solve on this device.</p></div><div className="xp-orb"><span>XP</span><b>{data.xp}</b><small>earned</small></div></div><div className="stats-row"><div><span>OVERALL</span><b>{pct}%</b><small>path complete</small></div><div><span>LESSONS</span><b>{lessonDone}/{lessons.length}</b><small>concepts mastered</small></div><div><span>CHALLENGES</span><b>{challDone}/{challenges.length}</b><small>solutions validated</small></div><div><span>NEXT</span><b>{lessonDone<lessons.length?'Learn':'Explore'}</b><small>recommended action</small></div></div><div className="progress-grid"><section className="mastery-card"><div className="card-head"><div><div className="eyebrow">MASTERY MAP</div><h2>Quantum foundations</h2></div><span>{pct}%</span></div>{lessons.map((l,i)=><div className="mastery-row" key={l.id}><span>{String(i+1).padStart(2,'0')}</span><b>{l.title}</b><div><i style={{width:`${data.lessons[l.id]||0}%`}}/></div><strong>{data.lessons[l.id]||0}%</strong></div>)}</section><aside className="recommend-card"><div className="eyebrow">NEXT RECOMMENDATION</div><h2>{lessonDone<lessons.length?lessons[lessonDone].title:'Algorithm Studio'}</h2><p>{lessonDone<lessons.length?'Your next step builds directly on the concepts you have already completed.':'You have completed the learning path. Explore an algorithm and challenge yourself.'}</p><Link href={lessonDone<lessons.length?'/learn':'/algorithms'} className="primary-btn">Continue →</Link></aside></div></div>}
